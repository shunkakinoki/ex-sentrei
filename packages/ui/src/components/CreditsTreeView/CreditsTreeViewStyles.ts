import {createStyles, makeStyles} from "@material-ui/core/styles";

const CreditsTreeViewStyles = makeStyles(() =>
  createStyles({
    root: {
      flexGrow: 1,
      width: "100%",
    },
  }),
);

export default CreditsTreeViewStyles;
