import {createStyles, makeStyles} from "@material-ui/core/styles";

const LandingFeatureStyles = makeStyles(() =>
  createStyles({
    container: {
      display: "flex",
      flexGrow: 1,
    },
  }),
);

export default LandingFeatureStyles;
