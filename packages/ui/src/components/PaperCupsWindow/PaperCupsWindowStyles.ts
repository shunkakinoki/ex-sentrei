import {createStyles, makeStyles} from "@material-ui/core/styles";

const PaperCupsWindowStyles = makeStyles(() =>
  createStyles({
    window: {
      height: "70vh",
    },
  }),
);

export default PaperCupsWindowStyles;
