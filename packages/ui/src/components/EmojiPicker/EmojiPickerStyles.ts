import {createStyles, makeStyles} from "@material-ui/core/styles";

const EmojiPickerStyles = makeStyles(() =>
  createStyles({
    iconButton: {
      padding: 10,
    },
  }),
);

export default EmojiPickerStyles;
