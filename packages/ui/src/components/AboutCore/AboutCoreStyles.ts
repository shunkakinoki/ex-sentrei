import {createStyles, makeStyles} from "@material-ui/core/styles";

const AboutCoreStyles = makeStyles(() =>
  createStyles({
    container: {
      display: "flex",
      flexGrow: 1,
    },
  }),
);

export default AboutCoreStyles;
