import {shallow} from "enzyme";
import * as React from "react";

import LandingFooterTail from ".";

describe("LandingFooterTail", () => {
  test("render", () => {
    shallow(<LandingFooterTail />);
  });
});
