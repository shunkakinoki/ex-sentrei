import {createStyles, makeStyles} from "@material-ui/core/styles";

const AboutTeamStyles = makeStyles(() =>
  createStyles({
    container: {
      display: "flex",
      flexGrow: 1,
    },
  }),
);

export default AboutTeamStyles;
