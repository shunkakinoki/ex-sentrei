import {createStyles, makeStyles} from "@material-ui/core/styles";

const MemberListItemStyles = makeStyles(() =>
  createStyles({
    root: {
      display: "flex",
      alignItems: "center",
    },
  }),
);

export default MemberListItemStyles;
