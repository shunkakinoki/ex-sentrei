import {createStyles, makeStyles} from "@material-ui/core/styles";

const SpaceAnalyticsChartStyles = makeStyles(() =>
  createStyles({
    root: {
      height: "100%",
      position: "relative",
    },
  }),
);

export default SpaceAnalyticsChartStyles;
