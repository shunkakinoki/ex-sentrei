import {createStyles, makeStyles} from "@material-ui/core/styles";

const SpaceLeaderboardTableStyles = makeStyles(() =>
  createStyles({
    avatar: {
      display: "flex",
      alignItems: "center",
    },
  }),
);

export default SpaceLeaderboardTableStyles;
