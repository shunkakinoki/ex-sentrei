{
  "name": "@sentrei/functions",
  "version": "1.86.0",
  "homepage": "https://sentrei.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/sentrei/sentrei.git",
    "directory": "packages/functions"
  },
  "license": "MIT",
  "author": "Shun Kakinoki",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "alpha": "yarn run alpha:local && yarn run deploy",
    "alpha:local": "yarn run swap:alpha && firebase use alpha",
    "beta": "yarn run beta:local && yarn run deploy",
    "beta:local": "yarn run swap:beta && firebase use beta",
    "clean": "yarn run clean:dist && yarn run clean:tsbuildinfo",
    "clean:dist": "rimraf dist",
    "clean:tsbuildinfo": "rimraf tsconfig*tsbuildinfo",
    "config": "yarn run env:get:local && firebase functions:shell",
    "coverage:rules": "curl http://localhost:8080/emulator/v1/projects/sentrei:ruleCoverage.html > coverage/ruleCoverage.html",
    "depcheck": "depcheck .",
    "deploy": "yarn run deploy:rules",
    "deploy:functions": "firebase deploy -f --only functions",
    "deploy:functions:manual": "firebase deploy --only functions",
    "deploy:rules": "yarn run deploy:rules:database && yarn run deploy:rules:firestore && yarn run deploy:rules:storage",
    "deploy:rules:database": "firebase deploy -f --only database",
    "deploy:rules:database:manual": "firebase deploy --only database",
    "deploy:rules:firestore": "firebase deploy -f --only firestore",
    "deploy:rules:firestore:manual": "firebase deploy --only firestore",
    "deploy:rules:storage": "firebase deploy -f --only storage",
    "deploy:rules:storage:manual": "firebase deploy --only storage",
    "env:get": "firebase functions:config:get > .runtimeconfig.json",
    "env:set": "test -f env.json && firebase functions:config:unset env && firebase functions:config:set env=\"$(cat env.json)\"",
    "env:set:all": "yarn run env:set:alpha && yarn run env:set:beta && yarn run env:set:main",
    "env:set:alpha": "yarn run alpha:local && yarn run swap:alpha:env && yarn run env:set",
    "env:set:beta": "yarn run beta:local && yarn run swap:beta:env && yarn run env:set",
    "env:set:main": "yarn run main:local && yarn run swap:main:env && yarn run env:set",
    "eslint:check": "eslint . -c .eslintrc.json --ext .ts --ignore-path .gitignore --cache --cache-location ./node_modules/.cache/eslint",
    "eslint:fix": "eslint --fix . -c .eslintrc.json --ext .ts --ignore-path .gitignore --cache --cache-location ./node_modules/.cache/eslint",
    "firebase:alpha": "yarn run alpha:local && yarn run env:get && yarn run start",
    "firebase:beta": "yarn run beta:local && yarn run env:get && yarn run start",
    "firebase:main": "yarn run main:local && yarn run env:get && yarn run start",
    "firestore:export": "firebase emulators:export --force ./data --only firestore",
    "fix": "yarn run eslint:fix && yarn run prettier:fix && yarn run sort-package-json:fix",
    "indexes:get": "firebase firestore:indexes > firestore/firestore.indexes.json",
    "lint": "yarn run eslint:check && yarn run prettier:check && yarn run sort-package-json:check",
    "log": "firebase functions:log",
    "main": "yarn run main:local && yarn run deploy",
    "main:local": "yarn run swap:main && firebase use main",
    "prettier:check": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "release": "semantic-release",
    "release-test": "semantic-release -d",
    "setup": "firebase setup:emulators:firestore",
    "shell": "firebase functions:shell",
    "sort-package-json:check": "sort-package-json 'package.json' 'packages/*/package.json' --check",
    "sort-package-json:fix": "sort-package-json 'package.json' 'packages/*/package.json'",
    "start": "firebase emulators:start --import ./data --only firestore,functions,pubsub --inspect-functions",
    "swap:alpha": "cp alpha.firebase.json firebase.json",
    "swap:alpha:env": "cp alpha.env.json env.json",
    "swap:beta": "cp beta.firebase.json firebase.json",
    "swap:beta:env": "cp beta.env.json env.json",
    "swap:main": "cp main.firebase.json firebase.json",
    "swap:main:env": "cp main.env.json env.json",
    "test": "firebase emulators:exec --only firestore 'jest --silent && yarn run coverage:rules'",
    "test:watch": "firebase emulators:exec --only firestore 'jest --watch && yarn run coverage:rules'",
    "tsc": "tsc --p tsconfig.build.json",
    "tscpaths": "tscpaths -p tsconfig.json -s ./src -o ./dist",
    "typescript": "yarn run clean && yarn run tsc && yarn run tscpaths",
    "watch": "yarn run tsc -w --preserveWatchOutput"
  },
  "husky": {
    "hooks": {
      "prepare-commit-msg": "exec < /dev/tty && git-cz --hook || true"
    }
  },
  "dependencies": {
    "@firebase/testing": "^0.20.11",
    "@google-cloud/pubsub": "^2.5.0",
    "@sendgrid/mail": "^7.2.5",
    "@sentrei/common": "^1.52.1-beta.1",
    "@sentrei/types": "^1.52.1-beta.1",
    "@sentry/node": "^5.23.0",
    "@slack/web-api": "^5.11.0",
    "@types/analytics-node": "^3.1.3",
    "@types/lodash": "^4.14.161",
    "analytics-node": "^3.4.0-beta.2",
    "deep-equal": "^2.0.3",
    "firebase-admin": "^9.1.1",
    "firebase-functions": "^3.11.0",
    "firebase-functions-test": "^0.2.2",
    "firebase-tools": "^8.10.0",
    "jest": "^26.4.2",
    "jest-when": "^2.7.2",
    "lodash": "^4.17.20",
    "stripe": "8.94.0",
    "tscpaths": "^0.0.9",
    "twilio": "^3.49.1",
    "uuid": "^8.3.0"
  },
  "devDependencies": {
    "ts-jest": "^26.3.0"
  },
  "engines": {
    "node": "10",
    "npm": "please_use_yarn_instead"
  }
}
